# Autorise les scripts d'installation des paquets systemes (Electron doit
# telecharger son binaire via son postinstall, sinon il ne peut pas demarrer).
# Limite aux seuls paquets de confiance du projet.
enable-pre-post-scripts=true
